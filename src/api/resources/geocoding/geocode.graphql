type Geocode {
    id: ID!
    gurdwara: Gurdwara!
    latitude: String!
    longitude: String!
}

input NewGeocode {
  gurdwara: ID!
}

extend type Query {
    Geocode(id: ID!): Geocode!
    allGeocodes: [Geocode]!
}

extend type Mutation {
    newGeocode(input: NewGeocode!): Geocode!
}